  <section class="new-employer">
    <div class="container">
      <div class="row">
        <div class="col-xs-1 col-lg-4"></div>
        <div class="col-xs-10 col-lg-4 text-center">
          <h2 class="take-out-margin-t">Create company profile</h2>
        </div>
        <div class="col-xs-1 col-lg-4"></div>
      </div>


      <div class="row">
        <div class="col-xs-1 col-lg-3"></div>
        <div class="col-xs-10 col-lg-6">
          <div class="row">

            <%= form_for @employer, url: {action: "create"} do |form| %>
        <div class="form-group">
          <%= form.label :icon_url, "Logo:" %>
          <%= form.file_field :icon_url %>
        </div>
          <div class="form-group">
          <%= form.label :name, "Company name:" %>
          <%= form.text_field :name, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= form.label :description, "Description:" %>
          <%= form.text_area :description, class: 'form-control' %>
        </div>

        <%= form.submit 'Create account', class: 'btn btn-primary' %>
        <% if form.object.errors.any? %>
          <div class="alert alert-danger">
            <strong>The following errors prevented saving:</strong>
            <ul>
              <% form.object.errors.full_messages.each do |error| %>
                <li><%= error %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

      <% end %>

       </div>
    <div class="col-xs-1 col-lg-3"></div>
    </div>
    </div>
  </section>